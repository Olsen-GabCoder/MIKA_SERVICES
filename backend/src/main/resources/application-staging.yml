# Configuration Staging
spring:
  jpa:
    hibernate:
      ddl-auto: validate   # Vérifie que le schéma correspond
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  logging:
    level:
      com.mikaservices.platform: INFO
