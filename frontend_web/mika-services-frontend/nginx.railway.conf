# Template : listen PORT sera remplacé au démarrage par envsubst (variable Railway PORT).
server {
    listen __PORT__;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
}
