# Configuration Développement
# Import barème : charge l'Excel et remplit tous les champs vides (—, 0, ou valeur par défaut).
# Pour peuplement sans Excel (données 100 % synthétiques), mettre seed-enabled: true et path: ""
bareme:
  seed-enabled: false
  import:
    path: ../docs/BAREME DES PRIX BÂTIMENT AVEC SOUS DETAILS (Tout_Corps_d'Etat).xls

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mika_services_dev?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: olsenk2000#2000
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    com.mikaservices.platform: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE